- hosts: all
  become: yes
  tasks:
    - name:
      yum:
        name: httpd
        state: latest


[defaults]
inventory = inventory
remote_user = ec2-user
become = true
become_method = sudo
beome_user = root

c=a!=b and a>b or b==10
